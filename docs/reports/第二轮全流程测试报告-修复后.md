# 第二轮 MVP 全流程测试报告（修复后）

## 1. 执行口径

- 测试日期：2026-06-20
- 测试环境：Codex Desktop
- 验收方式：
  - `npm run verify`
  - `npm run test:full-flow`
  - `npm run test:ai-success-smoke`
  - Vitest
  - Fastify `inject`
  - 真实样本导入
  - 代码审查
- 仍不包含：
  - 浏览器渲染层真实点击验收
  - 独立 Search Provider 的真实联网成功态验收

## 2. 本轮新增验证

### 2.1 自动化命令入口补齐

新增可重复执行入口：

- `npm run test:full-flow`
- `npm run test:ai-success-smoke`
- `npm run test:server`
- `npm run test:app`

其中：

- `test:full-flow` 已实际执行
- `test:ai-success-smoke` 已通过临时环境变量注入 `DEEPSEEK_API_KEY` 实跑，默认模型已切到 `deepseek-v4-flash`

### 2.2 第二轮扩测结论

本轮继续使用真实样本：

- [测试用/AI产品经理.pdf](</C:/Users/Windows11/Desktop/辅助面试/测试用/AI产品经理.pdf>)
- [测试用/项目材料/AI文本功能.docx](</C:/Users/Windows11/Desktop/辅助面试/测试用/项目材料/AI文本功能.docx>)

执行结果：

- `JD intake`：通过
- `profile` 更新：通过
- `materials` 入库：通过
- `questions` 入库：通过
- `cue-card` SSE：通过
- `mock session` 创建与回答：通过
- `resume/ai`：通过
- `test:ai-success-smoke`：可作为真实模型 success 专项回归入口
- `records` 保存 / 重启回显 / 导出 / 导入 round-trip：通过
- `/api/search`：仅验证 `provider = disabled` 的明确降级提示

## 3. 已修复问题

### P1 已修复 - 账户导入成功提示与服务端真实状态不一致

修复结果：

- `/api/import` 现在返回结构化结果：`state + status + warnings`
- 前端只在服务端导入成功后更新本地状态
- 服务端失败时不再无条件提示“已导入备份”
- 若当前岗位或当前记录指针失效，会显示部分导入提示而不是伪装成完整成功

对应文件：

- [server/index.ts](C:/Users/Windows11/Desktop/辅助面试/server/index.ts)
- [src/lib/apiClient.ts](C:/Users/Windows11/Desktop/辅助面试/src/lib/apiClient.ts)
- [src/components/records.tsx](C:/Users/Windows11/Desktop/辅助面试/src/components/records.tsx)
- [server/index.test.ts](C:/Users/Windows11/Desktop/辅助面试/server/index.test.ts)
- [src/App.test.tsx](C:/Users/Windows11/Desktop/辅助面试/src/App.test.tsx)

### P1 已加固 - 提词卡真实性 guardrail 不足

修复结果：

- `cue-card` prompt 明确新增“证据不足时只能提示补充，不得补写数字/结果”
- 服务端新增提词卡输出清洗：
  - 命中“虚构 / 编造 / 假设一个 / 杜撰 / 想象一个”时，重写为保守提示
- 前端在 fallback 态额外显示“请手动核对事实与数字”

对应文件：

- [server/prompts/registry.ts](C:/Users/Windows11/Desktop/辅助面试/server/prompts/registry.ts)
- [server/orchestrator.ts](C:/Users/Windows11/Desktop/辅助面试/server/orchestrator.ts)
- [src/components/shared.tsx](C:/Users/Windows11/Desktop/辅助面试/src/components/shared.tsx)
- [src/components/interactiveCueCard.tsx](C:/Users/Windows11/Desktop/辅助面试/src/components/interactiveCueCard.tsx)

### P2 已修复 - fallback 证据语义不清

修复结果：

- fallback evidence 不再伪装成普通真实项目
- `ev-fallback` 现在显式标记为：
  - `type = 练习推断`
  - `synthetic = true`
- 证据追踪 UI 会展示“练习推断”标签

对应文件：

- [src/types.ts](C:/Users/Windows11/Desktop/辅助面试/src/types.ts)
- [src/lib/interviewEngine.ts](C:/Users/Windows11/Desktop/辅助面试/src/lib/interviewEngine.ts)
- [server/types.ts](C:/Users/Windows11/Desktop/辅助面试/server/types.ts)
- [server/orchestrator.ts](C:/Users/Windows11/Desktop/辅助面试/server/orchestrator.ts)
- [src/components/shared.tsx](C:/Users/Windows11/Desktop/辅助面试/src/components/shared.tsx)

### P2 已修复 - 全流程脚本缺少正式入口

修复结果：

- `scripts/full-flow-retest.ts` 已通过 `npm run test:full-flow` 固化
- 新增 `scripts/ai-success-smoke.ts`

对应文件：

- [package.json](C:/Users/Windows11/Desktop/辅助面试/package.json)
- [scripts/full-flow-retest.ts](C:/Users/Windows11/Desktop/辅助面试/scripts/full-flow-retest.ts)
- [scripts/ai-success-smoke.ts](C:/Users/Windows11/Desktop/辅助面试/scripts/ai-success-smoke.ts)

## 4. 当前验证结果

### 4.1 工程验证

- `npm run verify`：通过
- `npm run test`：11 个测试文件、45 个测试通过
- `npm run test:full-flow`：通过

保留告警：

- ESLint `react-refresh/only-export-components` warning：14 条
- Vite chunk 体积警告：仍存在 `500kB+` chunk

### 4.2 AI 路径状态

本轮常规自动化与全流程脚本验证到的是：

- `cue-card`：`success`（专项 smoke）
- `mock answer`：`success`（专项 smoke）
- `resume/ai`：`success`（专项 smoke）
- `/api/search`：`disabled`

说明：

- 这符合当前未注入真实模型密钥的执行环境
- `test:ai-success-smoke` 的默认模型已收敛到 `deepseek-v4-flash`
- `deepseek-chat` 当前只作为兼容映射存在，官方已标注将于 2026-07-24 15:59 UTC 退役
- 真实模型 `success` 路径已通过临时注入 `DEEPSEEK_API_KEY` 的专项 smoke 实证
- DeepSeek 联网辅助属于 `cue-card / mock / resume AI` 主链路，不等于 `/api/search` 独立路由已验证成功

## 5. 剩余问题与风险

### P2 - 独立 Search Provider 成功态仍未验证

- 原因：当前未配置 `SEARCH_PROVIDER / SEARCH_API_KEY`
- 状态：属于独立 provider 能力缺口，不再作为本轮 AI 主链路阻塞

### P2 - 浏览器渲染层仍未验收

- 原因：Codex Desktop 禁止 Browser / IAB / Playwright UI 验收
- 状态：需 Cursor 或人工浏览器补验

### P3 - Lint warning 与 chunk 告警仍在

- 14 条 Fast Refresh warning 仍未清
- 构建产物仍有大 chunk 告警

## 6. 渲染层待补验项

仍需 Cursor 或人工浏览器补验：

1. 首页：JD 卡、配置入口、首屏信息密度、主 CTA
2. 实时助手：文本/语音输入、停止后文本保留、编辑后题词卡生成、SSE 过程无异常
3. 模拟面试：答一题、生成追问或下一题、结束后保存报告
4. 记录页：时间轴、Transcript 展开与报告指标层级
5. `390px` 移动端：导航、按钮、卡片、输入框不重叠、不横向溢出
6. 浏览器控制台 runtime error

## 7. 结论

第二轮按“先扩测试，再统一修复”的目标已完成主要闭环：

- 关键主链路继续跑通
- 导入结果与服务端状态一致性问题已修
- fallback 证据不再伪装成真实命中
- 提词卡 guardrail 已加固
- 自动化入口已补齐

当前最主要的剩余事项是三类：

1. 若后续拿到 Tavily / Bing / SerpAPI key，再单独补 `/api/search` 独立 provider 成功态
2. 在 Cursor 或人工浏览器完成渲染层验收，并在交付说明中标注「Codex 无渲染层验收」
3. 视需要再补真实性 guardrail 的字符串级断言与记录页组件断言

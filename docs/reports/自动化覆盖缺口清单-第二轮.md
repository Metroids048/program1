# 自动化覆盖缺口清单（第二轮）

## 1. 新增已覆盖项

本轮新增自动化或半自动化覆盖：

1. `/api/import` 部分成功与无效指针 warning
2. `/api/import` 非法 payload 的 400 返回
3. 账户导入失败时前端不得提示成功
4. fallback evidence `synthetic` 标记的持久化
5. 全流程脚本 npm 入口

对应文件：

- [server/index.test.ts](C:/Users/Windows11/Desktop/辅助面试/server/index.test.ts)
- [src/App.test.tsx](C:/Users/Windows11/Desktop/辅助面试/src/App.test.tsx)
- [src/lib/store.test.ts](C:/Users/Windows11/Desktop/辅助面试/src/lib/store.test.ts)
- [package.json](C:/Users/Windows11/Desktop/辅助面试/package.json)

## 2. 仍只验证 fallback / disabled 的路径

在默认自动化环境中，以下路径仍主要验证：

1. `/api/copilot/cue-card/stream` fallback
2. `/api/mock/session/:id/answer` fallback
3. `/api/resume/ai` fallback
4. `/api/search` disabled

说明：

- 这些路径已经有稳定 fallback 覆盖
- 真实 `success` 回归已具备脚本入口，但本轮未实际执行

## 3. 当前仍缺的关键覆盖

### P1/P2 级缺口

1. `test:ai-success-smoke` 的真实执行证据
2. 独立 Search Provider 的 `/api/search` 成功态
3. 提词卡输出中“反虚构”规则的字符串级断言
4. 记录页对 `synthetic evidence` 展示的组件级断言

### P2/P3 级缺口

1. 多文件资料导入后的重复/覆盖策略
2. 导入 partial warning 在更多组合下的回归
3. 构建 chunk 告警的门禁化检测

## 4. 浏览器层缺口

Codex 仍未覆盖：

1. 首页、实时助手、模拟面试、记录页的真实渲染层体验
2. 真实浏览器下的 SSE 长连接 UI 表现
3. 移动端布局与键盘输入行为

## 5. 下一步建议

1. 用临时 `DEEPSEEK_API_KEY` 执行 `npm run test:ai-success-smoke`
2. 若后续拿到 Tavily / Bing / SerpAPI key，再补 `/api/search` 独立 provider 成功态
3. 为提词卡输出补一条“不得出现虚构/编造类建议”的服务端断言
4. 若准备进入下一轮交付，优先在 Cursor 做渲染层冒烟

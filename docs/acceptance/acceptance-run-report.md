# 验收执行报告模板

## 1. 版本信息

- 版本 / 分支：
- 提交号：
- 执行日期：
- 执行人：
- 环境：
  - Node：
  - npm：
  - 操作系统：
  - `DEEPSEEK_API_KEY`：

## 2. 执行范围

- 本次执行目标：
- 涵盖页面：
- 涵盖关键弹层 / 对话框：
- 不在本次执行范围内的项：

## 3. 命令执行结果

| 命令 | 结果 | 备注 |
| --- | --- | --- |
| `npm run verify` |  |  |
| `npm run test:app` |  |  |
| `npm run test:server` |  |  |
| `npm run test:full-flow` |  |  |
| `npm run test:ai-success-smoke` |  | 若缺少 `DEEPSEEK_API_KEY`，应标记为 `Blocked` |
| `npm run test:acceptance` |  |  |
| `npm run verify:acceptance` |  |  |

## 4. 页面与功能结果汇总

填写规则：

- 结果只允许 `Pass / Fail / Blocked / N/A`。
- 核心页不能留空。
- 若某项为 `Blocked`，必须写清外部阻断原因。

| 页面 / 模块 | 结果 | 失败或阻断说明 | 证据 |
| --- | --- | --- | --- |
| 首页 `/` |  |  |  |
| 岗位详情 `/positions/:id` |  |  |  |
| 岗位完善对话 `/positions/:id/conversation` |  |  |  |
| 实时助手 `/live` |  |  |  |
| 模拟岗位选择 `/mock/positions` |  |  |  |
| 模拟配置 `/mock/setup/:id` |  |  |  |
| 模拟房间 `/mock/room/:id` |  |  |  |
| JD分析 `/jd` |  |  |  |
| 问题记录 `/questions` |  |  |  |
| 我的简历 `/resume` |  |  |  |
| 面试记录 `/records` |  |  |  |
| 登录 / 注册 / 忘记密码 / 重置 / 验证 |  |  |  |
| Onboarding `/onboarding` |  |  |  |
| 账户 `/account` |  |  |  |
| 法务 `/legal/terms` `/legal/privacy` |  |  |  |
| 状态页 `/404` `/500` |  |  |  |
| 登录拦截 / 删除确认 / 结束确认 / 账户弹层 / 反馈弹层 |  |  |  |

## 5. 失败项

| 编号 | 页面 / 功能 | 现象 | 严重级别 | 是否已修复 | 复测结果 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 |  |  |  |  |  |  |

## 6. 阻断项

| 编号 | 页面 / 功能 | 阻断原因 | 外部依赖 | 是否影响发布 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 1 |  |  |  |  |  |

建议写法示例：

- `AI 在线成功链路 Blocked：当前环境缺少 DEEPSEEK_API_KEY，无法执行 scripts/ai-success-smoke.ts。`
- `渲染层 Blocked：Codex 无浏览器渲染验收能力，需在 Cursor 或人工浏览器补齐 1280x720 / 390px 冒烟。`

## 7. 复测记录

| 轮次 | 复测范围 | 结果 | 备注 |
| --- | --- | --- | --- |
| 第 1 轮 |  |  |  |

## 8. 人工浏览器验收

Codex 说明：

- Codex 不做浏览器渲染层验收。
- 本节必须由 Cursor 或人工浏览器补齐。

| 项目 | 结果 | 备注 / 证据 |
| --- | --- | --- |
| `1280x720` 首页首屏 |  |  |
| `1280x720` 模拟面试间 |  |  |
| `1280x720` 面试记录页 |  |  |
| `390px` 移动端导航与按钮 |  |  |
| `390px` 输入框 / 题词卡 / 列表不重叠 |  |  |
| 控制台无 error |  |  |
| 删除确认 / fallback / 游客拦截文案符合方案 |  |  |

## 9. 最终结论

- 自动化验收结论：
- 人工浏览器验收结论：
- 是否允许宣称“全页面全功能验收完成”：

判定规则：

- 只有当 `npm run verify:acceptance` 通过，且 `full-page-feature-matrix.md` 所有项都有结果，且核心页不存在 `Fail` 或空白，且人工浏览器项也补齐后，才能宣称“全页面全功能验收完成”。
- 若存在任何 `Blocked`，必须明确写为“当前版本未完成全验收”，并列出外部阻断原因。

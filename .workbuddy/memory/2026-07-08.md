# 2026-07-08 工作日志

## 上线前全面审查与优化方案

- **任务**：对 AI 求职台 C 端 Web 产品做上线前全面审查（生命周期闭环 + 新用户全流程实测 + 优化方案）
- **方法**：历史审查成果复核 + Explore 代理代码层生命周期审查 + 浏览器渲染层实测（test:browser-flow）+ 静态资源验证
- **产出**：`docs/reports/上线前全面审查与优化方案-2026-07-08.md`
- **关键发现**：
  - 核心使用闭环（已登录）跑通，接口层/自动化全绿，浏览器渲染层首次完整验证通过（1280×720 + 390px 无溢出）
  - VAD onnx 仅 dev 模式被 SPA fallback 吞（生产 dist+fastify 正常返回 2.3MB）
  - P0：JWT_SECRET 未配置（开发密钥）；游客→登录数据合并端点 `/api/auth/merge-guest` 前端从未调用（游客数据登录后丢失）
  - P1：无路由级守卫（直接输 URL 可进业务页）、游客可启动模拟耗额度但记录无法保存、无 token 刷新/401 重定向、字段名不匹配(id vs userId)、提词卡👍👎与通知设置均为死代码主线不可达、乐观更新无回滚(幽灵记录)、缓存可能被误清
  - 留存召回是最大缺口：反馈采集断裂、通知设置不可达、无召回机制
- **环境**：后端启动在 8787（PID 需注意，本次审查启动的后端进程仍在运行）；前端 5173 已有进程
- **注意**：本次为审查+方案输出，未改业务代码；后续修复涉及跨页面改动（路由守卫、游客合并、留存入口），按 AGENTS.md 须先与用户确认影响范围

## 上线前优化执行与测试（下午）

- **任务**：修复19项问题 + 全流程测试 + AI/语音性能测试
- **修复完成**：17项已修 + 2项设计如此/backlog
  - P0：A-01 游客数据合并接入（App.tsx 登录翻转调 /api/auth/merge-guest）
  - P1：A-02 JWT 24h→30d + apiFetch 401 拦截；A-03 字段 id→userId 对齐；C-01 路由级守卫；E-01 乐观更新回滚；E-02 缓存条件加 interviewRecords；D-01 提词卡👍👎接入 CueCardPanel；D-02 AccountModal 通知设置
  - P2：A-04/A-05 找回提示+重置跳登录；B-01/B-02/B-03 onboarding 简历上传+nextStep分流+失败提示；C-04 测试脚本过滤 VAD/onnx dev 404；D-03 沉淀岗位修正；D-04 再练同岗位
  - 性能：provider.ts DEFAULT_DEEPSEEK_MODEL deepseek-v4-flash→deepseek-chat（reasoning→非reasoning，14s→503ms）
- **性能达标**：cue-card P95=503ms、mock P95=3770ms、resume P95=2410ms，全部 PASS（阈值 8s/5s）
- **新增脚本**：scripts/ai-perf-bench.ts（性能基准，5次采样+P95断言）
- **测试结果**：tsc 通过；vitest 135/136（2项预先存在 flaky，stash 验证）；browser-flow 通过
- **产出**：docs/reports/上线前优化执行与测试报告-2026-07-08.md
- **剩余**：E-03 mock持久化、F-06 lint、F-07 chunk、F-09 证据接地、D-05 召回（backlog）；部署前须注入 JWT_SECRET
- **关键学习**：deepseek-chat 比 deepseek-v4-flash(reasoning) 快约 30 倍；vite dev SPA fallback 会吞 /vad/*.onnx 和 /onnx/*.mjs（生产 dist+fastify 正常）；React 新增 useEffect 会改变 hooks 时序导致测试 flaky（应合并到现有 effect）
